# assignment operators are used to assign values to variables.
"""
1.Basic operators
(= assign the value on the ride hand side to the value of left hand side)
a = 10
2. augmented assignment operators
(+=,-=,%=,*= we can update a varible's value in a concise way)
bitwise (|=,&=,^=,>>=,<<=)
3. specialized operators
(:= walrus operator=>it enables assigning values within expressions, 
such as if (n := len(list)) > 0:)
"""
